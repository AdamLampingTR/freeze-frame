// Re-export the canonical contract (type-only, erased at build).
export type {
  FlagStatus,
  SkipKind,
  SkipReason,
  NotifyVia,
  Ticket,
  FreezeCandidate,
  SkipEntry,
  Stats,
  CandidatesResponse,
} from "../../shared/types";
